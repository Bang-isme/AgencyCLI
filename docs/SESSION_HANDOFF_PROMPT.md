# AgencyCLI — Prompt tiếp nối phiên (nắm rõ source, chống trùng lặp)

> **Cách dùng:** dán nguyên khối dưới đây vào tin nhắn đầu của một phiên Claude Code mới
> trên repo này. Prompt **trỏ tới** `docs/` + `memory/` thật (không sao chép nội dung) nên
> không tự trở thành bản trùng lặp và luôn cập nhật theo docs. Số liệu trong mục 6 là ảnh
> chụp tại thời điểm soạn — prompt đã yêu cầu phiên mới xác nhận lại bằng docs/memory.

---

Bạn tiếp quản **AgencyCLI** (`D:\AgencyCLI`) — monorepo pnpm 16 package, runtime agent tự hành cục bộ (KHÔNG phải chatbot), đang trong chiến dịch **production-hardening**. Hai "bệnh" cốt lõi của repo: (1) **machinery xây xong nhưng không nối dây** (built-but-unwired) và (2) **trùng lặp logic/file/UI/kiến trúc**. Nhiệm vụ: tiếp tục từng slice nhỏ, chất lượng cao, **tuyệt đối không tạo trùng lặp** và **không xây thứ treo lơ lửng**.

## 0. ĐỌC TRƯỚC khi làm bất cứ gì (đúng thứ tự)
1. `memory/MEMORY.md` + `memory/agencycli-production-hardening.md` — nhật ký chạy + git chain + trạng thái mới nhất.
2. `docs/HARDENING_HANDOFF.md` — §3 (đã xong + số test/package), §5 (STATUS + các note "cont'd N" = nhật ký từng slice), §6 (git), §7 (resume).
3. `docs/ROADMAP_HANDOFF.md` — kế hoạch 6 bậc + các mục §2.x (đã đánh dấu ✅/🟡).
4. **`docs/PACKAGES.md` → mục "Canonical Homes & No-Duplication Map"** — BẮT BUỘC: "ai sở hữu cái gì" + bảng "trùng tên/khác ngữ nghĩa CỐ Ý — KHÔNG gộp" + lệnh quét trùng.

> Không suy diễn từ trí nhớ — số liệu/cấu trúc có thể đã đổi. Luôn `grep`/đọc file THẬT rồi mới kết luận. **Bài học nhãn cũ:** `OutputEngine` từng bị gán "dead" nhưng thực ra LIVE; `tui/state/semantic-orchestration.ts` từng bị grep `--include='*.ts'` báo "dead" nhưng WIRED qua `App.tsx` (đuôi `.tsx` bị bỏ qua). **Khi kiểm importer luôn quét CẢ `.ts` + `.tsx` + `.mts`** kẻo xóa nhầm code live.

## 1. CHỐNG TRÙNG LẶP (trọng tâm)
- Trước khi thêm BẤT KỲ helper/module/tool/command nào → tra "Canonical Homes". Concern đã có nhà → **tái dùng**, không viết lại.
- Chạy quét bằng chứng (bản đầy đủ trong PACKAGES.md):
  ```bash
  grep -rEn --include='*.ts' "^export (async )?(function|class) [A-Za-z0-9_]+" packages/*/src \
   | grep -v __tests__ \
   | sed -E 's#(.*):[0-9]+:export (async )?(function|class) ([A-Za-z0-9_]+).*#\4\t\1#' \
   | sort | awk -F'\t' '{n[$1]=n[$1]" "$2;c[$1]++} END{for(k in c) if(c[k]>1) print c[k]"  "k":"n[k]}'
  ```
- Phân loại MỖI lần:
  - **trùng THẬT** (logic y hệt) → gộp vào canonical home, **behavior-preserving** (giữ nguyên precedence/edge-case).
  - **trùng tên/khác ngữ nghĩa CỐ Ý** → GIỮ, không gộp: `ReplayEngine` (core journal ↔ telemetry trace), `truncateText` (TUI width-aware ↔ core char-count), token `~len/4` (mỗi package công thức khác), `handover.ts` (core logic ↔ cli command), per-package `index/types/config/runner`.
- Nhà chung đã thiết lập: logic dùng chung 2 turn path → `chat/turn-helpers.ts`; output headless → `OutputEngine` (qua `cli/src/utils.ts` `out`/`handleError`); tool-loop breaker → `chat/circuit-breaker.ts`; DAG/plan → `task/runner.ts` `runPlan`; routing → `agents/agent-registry.ts`; journal+replay → `events/`; precise AST edit → `utils/ast-compiler.ts` (qua tool `ast_edit`); "tool nào ghi file" → `isFileWritingTool`; trace record/replay → telemetry tracker + benchmark `runRegressionReplay` (driver = lệnh `agency replay-regression`); **cognition narration (`thought:emitted` cho CognitionPanel) → `events/cognition.ts` `emitThought`** (producer DUY NHẤT, gate `cognitionStream`; ĐỪNG publish `thought:emitted` thủ công; điểm emit: routing, safety gating, capability reroute, verify self-heal qua `emitVerifyRoundThought`, compaction); **hiển thị flag ở `agency status` (human) → `cli/commands/status.ts` `buildFlagRows`** (1 list khai báo, ĐỪNG `console.log` flag lẻ); `ChatMessage` (`{role,content}`) **đã về `@agency/providers`** (core re-export, đừng khai lại).
- **Cặp trùng-tên CỐ Ý ở tầng type/interface (KHÔNG gộp):** `ToolCall` (core parsed-XML `{name,arguments:string}` ↔ tooling `{id,…,any}`), `GraphEdge` (code graph ↔ memory knowledge graph), `VerificationResult` (harness command ↔ checkpoint task), `AuditEntry` (approval gate ↔ memory mutation). Quét type: đổi `(function|class)` thành `(interface|type)` trong lệnh trên.

## 2. WIRED-OR-DEAD — đừng tái tạo nợ
Mọi machinery export từ `core/index.ts` đều WIRED hoặc DELETED (đóng 100%). Audit ĐÃ MỞ RỘNG ra cả file `core/src` KHÔNG nằm trong index (quét full + kiểm importer `.ts`/`.tsx`/`.mts`): đã xóa 2 module speculative (`skill/context-delivery.ts`, `validation/correctness-science.ts`) và wire 1 producer chết (`events/cognition.ts`). Code mới phải **được nối dây + dùng ngay**, không "xây rồi treo". Tool mới auto-quảng bá cho model qua `registry.listTools()`→`buildSystemPrompt` (đăng ký = model thấy). Nếu thấy gì giống dead-code → grep 0 live importer (CẢ `.tsx`) rồi mới xử lý; phân biệt **dead thật** (xóa) vs **live-consumer/dead-producer** (wire, như cognition + §2.5); ghi vào docs.
- **6 tầng wiring/kiến trúc đã có REGRESSION GUARD (chạy trong `pnpm verify`) — đừng để drift:** skills↔manifest (`cli/__tests__/skills-manifest-integrity.test.ts`: mọi skill khai có SKILL.md + ngược lại + `manifest.agents`==`MANIFEST_AGENTS` + load_order); agents↔prompt/seed (`core/__tests__/agent-dispatch-integrity.test.ts`: `AGENT_SUBAGENT_PROMPT`/seed/`MANIFEST_AGENTS` đồng bộ + prompt file resolve + disciplines là skill thật); flags↔status (`cli/__tests__/status-flags.test.ts`: mọi flag `getRuntimeFlags()` đều xuất hiện ở `agency status`); **architecture↔cycles MỨC MODULE (`core/__tests__/architecture-cycles.test.ts`: Tarjan-SCC trên `core/src`, fail nếu MODULE NGOÀI cycle chức năng 7-module known lọt vào runtime import cycle — bắt lỗi "tầng dưới import lên `chat/orchestrator` lấy helper"; fix bằng dời helper sang leaf, KHÔNG nới set); architecture↔cycles MỨC PACKAGE (`cli/__tests__/package-cycles.test.ts`: Tarjan-SCC trên các edge `@agency/<pkg>`, fail nếu có BẤT KỲ cycle giữa các package — fix bằng đảo back-edge: dời concern xuống leaf hoặc inject từ caller); **deps↔imports hygiene** (`cli/src/__tests__/package-cycles.test.ts` describe `package dependency hygiene`, dùng chung scaffolding với cycle guard: mỗi package, `@agency/*` khai trong `dependencies` PHẢI == `@agency/*` thực sự import trong `src` — bắt **dead dep** (khai mà 0 import, vd `@agency/cli` từng treo `@agency/telemetry` mà tsc resolve trace type transitively qua `@agency/benchmark`) lẫn **phantom dep** (import mà không khai, chỉ sống nhờ pnpm hoisting); bắt MỌI dạng import gồm dynamic `import("@agency/x")`).** **Thêm skill/agent/flag mới → cập nhật manifest/seed/buildFlagRows trong CÙNG slice** kẻo guard fail. Runtime: `agency doctor` cũng validate pack ĐÃ CÀI (mọi skill khai resolve SKILL.md).
- **Harness/tools/skills/agents inventory** đã ghi sẵn ở `PACKAGES.md` mục "Harness, built-in tools & skills" (17 built-in tool trong MỘT `ToolRegistry`, vòng lặp turn, 28 skill + 8 agent + 8 workflow) — ĐỌC thay vì tự suy ra lại.

## 3. VERIFY = sự thật, đừng assert
- Gate DUY NHẤT: `pnpm verify` (= `pnpm -r build && pnpm -r test`). Baseline (2026-06-01, sau P0 §8 + §8.5): build 16/16 sạch, **~2063 test, REAL_EXIT_CODE=0** (core 351 / cli 573 / tui 124 / providers 850 / memory 36 / benchmark 18 / workspace 11 / security 35 / tooling 14 / governance 7 / skills-bridge 14 / context 6 / heuristics 6 / browser 5 / telemetry 9).
- **Chạy `pnpm verify` TRƯỚC khi nói "green" hay commit.** Repo có lịch sử "xanh ảo" — KHÔNG được assert. (Kiểm `EXIT_CODE=0`; bỏ qua warning cố ý: failover/rate-limit/Playwright-missing/docker-unreachable.)
- Test fail nghi flaky/timeout dưới tải đồng thời → **xác minh** bằng chạy riêng package đó; nếu flaky thật → **sửa gốc** (nới timeout), KHÔNG retry-cho-tới-khi-xanh.
- Thêm dep workspace mới → `pnpm install` trước khi build.

## 4. Quy ước flag & kiến trúc
- Thay đổi hành vi → giấu sau flag trong `runtime/flags.ts` (`AGENCY_PROFILE=legacy|hardened`; **legacy giữ hành vi cũ byte-identical**; risky = off-legacy/on-hardened; tốn-kém = opt-in off cả hai như `verifyTests`/`traceRecord`). Hiện **28 flag**.
- Tính năng thuần additive (command/tool mới, không đổi path cũ) → KHÔNG cần flag.
- Tránh import cycle: package phụ thuộc KHÔNG import flags từ core (dùng tham số/setter). Thêm cross-package dep → verify package đích là leaf/không tạo vòng. (Vd: cli đã khai `@agency/telemetry` leaf cho `replay-regression`.)
- Docs lịch sử `PRODUCTION_AUDIT*` = point-in-time → KHÔNG sửa. Chỉ cập nhật living docs (HARDENING_HANDOFF / ROADMAP / PACKAGES / CORE_ENGINE / CLI_REFERENCE / TESTING / **SESSION_HANDOFF_PROMPT**).

## 5. Nhịp mỗi slice (lặp lại)
investigate (đọc + grep) → phân loại (trùng thật vs distinct; wire vs delete) → thay đổi **tối thiểu, behavior-preserving, tái dùng canonical home** → thêm/cập nhật test → `pnpm verify` XANH → sync living docs (+ thêm row vào "Canonical Homes" nếu có nhà mới) → commit trên `master` (cây phải sạch) → cập nhật `memory/` + git chain trong MEMORY.md.

- Commit nhỏ từng slice; KHÔNG amend; KHÔNG `--no-verify`. Message kết bằng:
  ```
  Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
  ```

## 6. Trạng thái hiện tại & việc tiếp theo (xác nhận lại bằng docs/memory)
- Đã xong: P0+P1, audit gaps (B–F), §2.3 compaction, §2.4 tool layer (`ast_edit` + parallel an toàn), §2.5 **record producer** (`AGENCY_TRACE_RECORD` → `.agency/traces/`) **+ driver `agency replay-regression`**. Wired-or-dead đóng 100% (gồm mở rộng ngoài index). Các slice gần nhất: **emit cognition đủ 5 điểm** (routing/safety/reroute/verify-self-heal/compaction, cờ `AGENCY_COGNITION_STREAM`); **3 guard wiring** (skills/agents/flags, xem §2); **dedup**: gộp `ChatMessage` về providers, quét sạch const/type; **`agency doctor`** validate pack đã cài; **eval THẬT (BYOK)**: thêm bài counter-conventional `hard-merge-intervals` → **chứng minh verify-loop tự-sửa lỗi model end-to-end lần đầu** (hardened `rounds=2`, xem `EVAL_RESULTS.md` mục 2026-06-01). Git HEAD ≈ `fa41c3e` trên `master`, cây sạch. **26 flag.**
- **§2.5 LLM-response recording ĐÃ WIRE (2026-06-01, cont'd 18):** trace giờ ghi cả **LLM completion mỗi vòng** (`llmResponses` optional trên `DeterministicExecutionTrace`, tái dùng `ActiveTelemetryTracker`/`ReplayEngine`/`runRegressionReplay`/`SessionTraceRecorder` — KHÔNG machinery mới). `ReplayEngine.interceptLlmResponse(text)` (positional, mirror `interceptToolCall`) + `getUnconsumedLlmCount()`; `runRegressionReplay` + lệnh `agency replay-regression` giờ kiểm cả completion (drift nội dung/thiếu/thừa = exit≠0). Gate bằng cờ sẵn có `AGENCY_TRACE_RECORD` (off = recorder null = byte-identical; trace cũ không có field vẫn load). telemetry 4→9 / benchmark 14→18 / cli 565→568, ~2018.
- **Memory professionalism pass (2026-06-01, cont'd 19):** 3 slice (đều verify-green + commit) — (1) `0fe6371` **fix recall đa-session CLI CHẾT** (`resolveSessionId` rơi về hằng `"sess-cli"` → mọi run đụng 1 id → lọc `session_id != current` loại sạch episode CLI cũ) bằng id duy nhất per-process + thay raw `(db as any).db` bằng typed `recentEpisodesAcrossSessions` + char-budget; (2) `ffd0ae8` **wire `HybridRetriever` đang ngủ** (0 consumer live vì thiếu embedder) + `LocalDeterministicEmbedder` (feature-hashing offline/deterministic, sau interface `Embedder`), write-path embed episode→vector + read-path recall qua HybridRetriever, cờ MỚI `AGENCY_MEMORY_SEMANTIC` (off legacy/on hardened), `HybridRetriever` expose `source` (generic, giữ format episode); (3) `b42e82d` **compaction bound + chunk** (`summarizeMiddle` ≤ `maxInputChars`, chia chunk + gộp phân tầng → prompt tóm tắt không tự tràn). Git HEAD ≈ `b42e82d`, cây sạch. **27 flag.** ~2018→~2026.
- **§2.5 re-execution + running-summary ĐÃ XONG (2026-06-01, cont'd 20):** `20021fb` (re-execution lõi an toàn: `agency replay-regression --reexecute` re-derive tool-call từ `llmResponses` qua `parseToolCalls` THẬT, đối chiếu `toolOutputs`, drift→exit 1; KHÔNG side-effect; cli 568→571) + `19cf875` (running-summary xuyên lượt: `compactTurnHistory` nhận `cacheKey`=sessionId, lượt sau chỉ tóm turn MỚI gộp summary cũ, O(new); no cacheKey→byte-identical; core 343→344). **§2.5 đóng end-to-end; compaction = bound+chunk+incremental.** Git HEAD ≈ `19cf875`, ~2030, **27 flag**.
- **§2.3 IN-LOOP compaction + route-presentation cycle untangle (2026-06-01, cont'd 21):** đóng nốt follow-up "in-loop compaction" ROADMAP §2.3 hoãn. Nén giờ chạy TRƯỚC loop VÀ đầu MỖI vòng (gom thành closure `compactIfEnabled`, tái dùng `compactTurnHistory`, `cacheKey` làm incremental, no-op dưới ngưỡng + byte-identical khi off, KHÔNG cờ mới) — vì `turnHistory` lớn dần trong loop mà reactive handler chỉ thu window. Test mới `in-loop-compaction.test.ts` (+3). **Kèm gỡ import cycle THẬT:** dời 2 helper thuần `formatRouteSummary`/`buildSuggestedCommands` từ `chat/orchestrator.ts` sang leaf `chat/route-presentation.ts` (orchestrator re-export → path consumer giữ nguyên) để `context/pack.ts` + `agents/orchestrator.ts` không còn back-edge lên chat/orchestrator (vi phạm phân tầng); cạnh `agents→stream→orchestrator` (dispatch chạy turn) là chu trình chức năng hợp lệ → giữ. Behaviour-preserving + flag-gated. core 344→**347**, repo ~2030→**~2033**, **27 flag**. **§2.3 đóng trọn.**
- **Architecture cycle audit + guard (2026-06-01, cont'd 22):** Tarjan-SCC quét toàn `core/src` → 1 cụm cycle 8-module, còn 1 cạnh vi phạm phân tầng sót: `chat/presentation.ts` import `buildSuggestedCommands` từ `chat/orchestrator.ts` (consumer THỨ BA của helper đã dời ở cont'd 21 — đã sửa pack + agents, sót presentation). Repoint sang leaf `route-presentation.ts` (1 dòng, behaviour-preserving) → SCC **8→7**. Cụm 7-module còn lại là chu trình CHỨC NĂNG bất khả giản (turn↔tools↔dispatch↔turn + setup + gate) → giữ. Khoá bằng guard THỨ TƯ `core/__tests__/architecture-cycles.test.ts` (fail nếu module ngoài set 7 lọt vào cycle; negative-tested). core 347→**348**, repo ~2033→**~2034**, **27 flag**. 4 guard wiring/kiến trúc: skills/agents/flags/**cycles**.
- **Gỡ PACKAGE cycle `core ↔ skills-bridge` bằng dependency-inversion (2026-06-01, cont'd 23):** mở rộng audit cycle lên mức PACKAGE → đúng 1 cycle runtime `@agency/core ↔ @agency/skills-bridge` (smell đã biết, campaign từng *né*: đặt skills-manifest guard ở cli vì core không import được skills-bridge trong test). Cả cycle treo trên `skills-bridge/runner.ts` reach vào core lấy policy của `runTool` (`requiresApproval`+`appendAudit`+`EventBus`) — concern ORCHESTRATION nhét nhầm vào lib MECHANISM, lại còn inert (cả 2 caller `doctor`/`plugin` truyền `yes:true`+không `projectRoot` → không bao giờ warn/audit). Đảo phụ thuộc: `runTool` nhận hook tuỳ chọn `onBeforeRun(ctx)` (mechanism thuần; vắng ⇒ chạy thẳng), policy dời nguyên văn sang `cli/src/plugin-approval-gate.ts`, `doctor`/`plugin` inject. skills-bridge KHÔNG còn import `@agency/core` (gỡ luôn khỏi `dependencies`). Behaviour-preserving (gate byte-identical, vẫn fire cho caller non-`yes`; chỉ DỜI chứ không xoá năng lực bảo mật). Guard THỨ NĂM `cli/__tests__/package-cycles.test.ts` (Tarjan-SCC, assert 0 package cycle; negative-tested). cli 571→**572**, skills-bridge 13→**14**, repo ~2034→**~2036**, **27 flag**. **5 guard: skills/agents/flags/cycles-module/cycles-package.**
- **Symbol-level dead-export prune (2026-06-01, cont'd 24):** quét tầng MỊN nhất của thesis wired-or-dead — symbol export ở module sâu (ngoài `index.ts`) mà KHÔNG nơi nào tham chiếu (cùng bệnh built-but-unwired, mịn hơn audit cấp-module đã đóng). Quét 820 export non-index/non-test, kiểm importer CẢ `.ts/.tsx/.mts` + **gồm tests** → tách **truly-dead** (0 ref) khỏi **over-exported** (chỉ dùng trong CHÍNH file nó = vẫn LIVE, ĐỪNG xóa: `LeaseManager`/`globalLeaseManager`, corpus consts `tasks.ts`) → tìm **13 truly-dead**. Xóa hết (behaviour-preserving — 0 consumer kể cả test, `noUnusedLocals` xác nhận không orphan import; GIỮ sibling live `FailureNormalizer`/`touchProject`/`globalWorkerTracker`/`circuitBreakerState`/các helper `agent-modes` khác): `FailureClassifier`+`FailureCategory` (classifier suy đoán, đã có `FailureNormalizer` live), `getInstallCommand` (install không phải bước acceptance), `resetCircuitBreaker` (reset không ai gọi — `circuitBreakerState` vẫn live ở tool-loop), `addProject`+`removeProject` (đã có `touchProject` upsert), `modeSkillPrefix` (helper mode→skill-prefix chết trong module còn-live), `YOGA_SAFE_MARGIN`, `hasPendingStdinBytes`, `SemanticEvent`, `semanticEventBus` (singleton `EventBus` không dùng — gỡ luôn import `EventBus` duy nhất), `TemporalChoreographyEngine`/`globalChoreography` (liveness-heartbeat mồ côi — không ai ref singleton nên không bao giờ start; timer TUI thật đã thay). **LƯU Ý: sweep nền lần đầu bị cắt ở mốc abc `h…` nên SÓT `modeSkillPrefix`/`resetCircuitBreaker`/`semanticEventBus` — chạy LẠI trọn vẹn TRƯỚC commit mới bắt đủ (verify-đừng-assert).**
- **Dependency-hygiene audit + guard thứ 6 (2026-06-01, cont'd 25):** quét tiếp sang hygiene package-manifest — mỗi package, `@agency/*` khai trong `dependencies` vs `@agency/*` thực import trong `src`. Graph sạch TRỪ `@agency/cli` khai `@agency/telemetry` mà 0 import (chỉ 1 comment). Note cont'd-8 tưởng cli cần nó "để tsc resolve trace type" (`runRegressionReplay(trace: DeterministicExecutionTrace)` rò type telemetry qua signature `@agency/benchmark`). **Bác bỏ bằng thực nghiệm** (verify-đừng-assert): gỡ dep → `pnpm install` → rebuild cli sạch từ đầu → tsc resolve `DeterministicExecutionTrace` **transitively qua `node_modules` của chính `@agency/benchmark`** (nơi `.d.ts` import nó), KHÔNG qua cli. Vậy `@agency/telemetry` là **dead weight** (analogue cấp-package của dead-export / dọn `skills-bridge` cont'd 23) — gỡ khỏi `package.json` + `pnpm-lock.yaml` (diff 3 dòng). cli 0 runtime import telemetry, `replay-regression` tests pass không cần nó; sửa luôn comment sai (precedent `replay`/`@agency/contracts` bịa — `replay.ts` không hề đụng contracts). **Khoá bằng GUARD THỨ 6:** describe `package dependency hygiene` trong `package-cycles.test.ts` (dùng chung `packageDirs`/`listSourceFiles` — KHÔNG file/dup mới) assert declared==imported mỗi package (bắt dead + phantom dep; bắt MỌI dạng import gồm dynamic `import("@agency/x")` nên `await import("@agency/tui")` của cli không bị false-flag). Negative-tested: thêm lại dead dep → fail nêu `@agency/cli: declares "@agency/telemetry" but never imports it`. pnpm verify xanh, cli 572→**573**, ~2037, **27 flag**. **6 guard: skills↔manifest, agents↔prompt/seed, flags↔status, architecture↔cycles(module), package↔cycles, deps↔imports hygiene.**
- **Whole-file orphan sweep toàn package (2026-06-01, cont'd 26):** quét tầng GIỮA module-sweep core-only (cont'd 9) và symbol-sweep (cont'd 24): file mà export chỉ dùng TRONG chính nó (symbol-sweep coi live) nhưng KHÔNG ai import. Gom mọi basename module-specifier được import 1 lượt → flag file `src` non-entry nào basename không xuất hiện, TOÀN bộ package. Sạch trừ `vitest.config.ts` (×5, entry point hợp lệ → false-positive) + **`packages/memory/src/__benchmarks__/{benchmark-memory,chaos-stress}.ts`**: cả 2 self-executing (`runBenchmark()`/`runChaosStressBenchmark()` module-level) nhưng 0 ai chạy (no script/import/doc/CI), compiled vào `dist` ĐƯỢC SHIP (`files:["dist"]` = dead weight npm), `chaos-stress.ts` TRÙNG live `chaos-stress.test.ts` (8 test cùng kịch bản). **Xóa cả 2** (precedent cont'd 9; recoverable; muốn benchmark thì thêm lại WIRED — exclude khỏi production build + script `bench`). **GUARD cont'd-25 ĂN NGAY slice sau:** xóa `chaos-stress.ts` gỡ luôn import `@agency/governance` DUY NHẤT của memory → `pnpm verify` ĐẦU TIÊN FAIL đúng guard hygiene nêu `@agency/memory: declares "@agency/governance" but never imports it` → gỡ governance khỏi `memory/package.json` + lockfile (xác nhận 0 import src/test). Ghi sweep lặp trong PACKAGES.md (lint-style, KHÔNG guard CI — entry/config cần allowlist). pnpm verify xanh, counts không đổi, **27 flag**. Whole-file orphan sweep giờ RỖNG (trừ entry point).
- **HƯỚNG MỚI §8 — P0 overflow + TUI crash ĐÃ XONG (2026-06-01, commits `b83b55a`+`106ee22`):** trị crash BYOK thật (minimax NVIDIA: max 196608, gửi 197270, "auto-reducing" in mà vô hiệu). 3 fix cùng đợt: **§8.2** catalog provider-aware (`getCatalogSpec(model, providerId?)` clamp context xuống conservative robust-min cross-provider, bỏ outlier <50%max → minimax/nvidia ra **196608** không phải 204800-entry-sai; thread qua `getModelSpec`/`enrichWithCatalog`-siết-registry/`getTokenBudgetPlan`; gate cờ `AGENCY_MODEL_CATALOG`); **§8.3** estimator err-HIGH (`estimateMessagesTokens` `len/3.5`+overhead+non-string content); **§8.1** reactive cắt THÂN (`reduceHistoryToFit` dùng chung `stream.ts`+`orchestrator.ts`: repack system + `compactTurnHistory` + cắt/bỏ lượt lớn tới khi fit `newLimit*safety`, giữ system+message-cuối) + **chặn ratchet phá hoại** (honor parsedLimit thật thay vì `oldLimit*0.8` mỗi vòng — chính cái đẩy minimax 196608→16887 ghi đè đĩa). **Kèm crash TUI trong ảnh user:** `stripToolCalls(m.content)` không-guard ở `calculateFormattedLines` lúc render, content `undefined` (App `content: turn.body || undefined`) → "reading 'indexOf'" → ErrorBoundary loop; coerce non-string→"" tại canonical home. **Dọn override hỏng `16887`** trong `~/.agency/config.json` user (KHÔNG đụng API key). Test: model-catalog +5, error-parser mới (6), reduce-history mới (3), conversation +1, 2 test reactive cập nhật theo hành vi đúng. `pnpm verify` xanh 16/16 (providers 850 · core 351 · tui 116). Chi tiết: ROADMAP §8 (banner đầu mục).
- **§8.5 paste dài XONG (2026-06-01, commit `2172832`)** — VERIFY-đừng-assert: gốc thật = perf (`applyTextInput` append mỗi ký tự → O(n²) freeze paste 12k); sửa = gom 1 setBuffer O(n), behavior-preserving (+8 test `text-input.test.ts`, tui 116→124). **Cap chiều cao ĐÃ CÓ sẵn** (`PromptComposer` MAX_LINES=6 + "+N lines" indicator) → không làm lại. **Bracketed-paste KHÔNG bật** (Ink đã coalesce paste 1-event; `applyTextInput` reject `\x1b` nên `?2004h` sẽ vỡ paste). Paste→attachment (phần 3) để ngỏ (biên, vì cap đã bound hiển thị).
- **§8.10 TUI realtime activity — CHẨN ĐOÁN (user báo 2026-06-01, chưa code; xem ROADMAP §8.10 đầy đủ):** ảnh user cho thấy "đang làm gì chưa realtime + UX chưa chuyên nghiệp". **Gốc lớn nhất (A):** main turn KHÔNG phát event tool cấu trúc — nó nhồi text `[SYSTEM: Executing tool…]` vào stream LLM (`chat/stream.ts:70 formatToolCallNotice` + onDelta) rồi TUI **regex-parse lại** (`conversation/TraceTelemetry.tsx` + `utils/conversation/tool-labels.ts`) → vòng lossy structured→English→regex → nhãn sai (`list_dir·short video` vì `getGroundedTargetName` lấy first-string-value của args JSON), 2 dòng/tool, tương quan qua global `lastToolTargets` Map theo tên (vỡ khi parallel). `subagent:progress` cấu trúc CHỈ phát khi `agentId` set = chỉ subagent. **(B)** `activityPhase` chỉ "routing"→"writing"→"idle", KHÔNG đổi theo tool + heartbeat chỉ update khi `thought:emitted` → status kẹt "Writing 8m39s" khi đọc file. **(D)** 4 bề mặt chồng chéo (inline text-parse / subagent structured / `ExecutionPanel` cây phase HARDCODE giả / `CognitionPanel`). **(E)** `SystemActivityLine`≈`formatSystemActivityLine`+`toConciseTelemetry` = 3 bản render trùng. **SỬA:** event tool-lifecycle cấu trúc main turn (tái dùng EventBus/`emitThought`) → lái status realtime → gộp bề mặt + dedup. **▶ NEXT P1 = §8.10 và/hoặc §8.4 ảnh** (user ưu tiên; user đang test fix crash trước).
- **§8.11 Harness & built-in tools audit (token efficiency) — 2026-06-01 (xem ROADMAP §8.11):** **(A) ✅ ĐÃ SỬA `a550bd2`** — `truncateToolResult` model-aware scaling CHẾT (`require("@agency/providers")` trong ESM → throw → mọi model rơi default 30K chars; đo 200k/16k/no-model đều 30040). Sửa = import static `getModelSpec` + re-tune token-conscious (nhỏ<32K→8K, lớn≥200K→48K KHÔNG còn 400K dump, medium→32K), +4 test, core 351→355. **(B) token win lớn nhất — (1) reorder ✅ ĐÃ XONG, (2) Anthropic cache_control CÒN MỞ:** system prompt cố định ~2069 tok/turn xếp VARIABLE (goal pillars/intent) lên ĐẦU → phá automatic prefix-cache openai-compatible. **(1) ĐÃ SỬA (2026-06-01):** `buildSystemPrompt` (`chat/prompt.ts`) tách segment static/sessionAnchor/variableTail, cờ MỚI `AGENCY_PROMPT_CACHE`/`promptCachePrefix` (off-legacy byte-identical / on-hardened static-prefix-first → bật automatic prefix-cache mọi openai-compatible, 0 đụng adapter); pure reorder (length bất biến 2 mode); test `prompt-cache-order.test.ts` (+5), core 355→360, **28 flag**, `agency status` row. **(2) ✅ ĐÃ XONG (2026-06-01):** adapter Anthropic (`anthropic.ts`) `buildSystemField(system,cache)` dùng chung 2 method → on gắn `cache_control:{type:"ephemeral"}` vào `body.system` (Anthropic không có automatic prefix-cache); opt `cacheSystemPrompt?` thêm vào `CompleteOptions` (leaf), core set từ `promptCachePrefix` tại stream.ts + orchestrator.ts (summarizer bỏ qua); GA Claude 3+ không cần beta header. Reuse cờ (không cờ mới), test `anthropic.test.ts` +2, providers 850→852. **§8.11-B ĐÓNG TRỌN.** **(C)** "5-APPROACHES RULE" (`prompt.ts:77`) ép 5 hướng/turn = phí output token → mềm hoá (cờ). **(D)** tool-docs ~1109 tok/turn (sau B). **(E)** grep_file vs grep_search distinct nhưng tên dễ nhầm; ứng viên: patch tool + index-backed search. Thứ tự: B→C→D→E.
- **NEXT (backlog không-cần-key đã RỖNG):** (a) **promote `hardened`→default** — đích cuối, nhưng flip nhiều default hành vi cho MỌI user → cần (i) eval delta sạch (BYOK key, corpus model không one-shot) + (ii) user đồng ý rõ ràng; **KHÔNG tự flip default**; (b) §2.4 typed tool-result — **cố ý hoãn** (tool result đã truncate thông minh + LLM đọc text → typed là surface cao, lợi biên); (c) full live `ReplayProvider` re-run (surface cao: chặn tool-exec+gate+episode — để ngỏ).
- **Cần BYOK key (user cấp):** đo lại legacy↔hardened `agency eval --agent --suite hard --provider nvidia` để có rate-delta sạch (cần bài model fail attempt-1 ỔN ĐỊNH hoặc model yếu hơn) → rồi **promote `hardened`→default**.

### 6.1. Xử lý BYOK key AN TOÀN (nếu user cấp key để đo eval)
- **TUYỆT ĐỐI không ghi key thật xuống đĩa** (config/log/git/memory/docs). Cách đã dùng & verify sạch:
  1. backup `~/.agency/config.json` ra file tạm; đổi CHỈ `providers.nvidia.apiKey` thành placeholder `"${NVIDIA_API_KEY}"` (loader `resolveApiKey` nội suy `${VAR}` từ env).
  2. Đặt `trap 'cp "$BAK" "$CFG"; rm -f "$BAK"' EXIT` trong **cùng một** lệnh bash → config LUÔN khôi phục dù eval lỗi (env không persist giữa các lệnh bash → restore phải nằm trong cùng call).
  3. Truyền key qua env per-command: `NVIDIA_API_KEY=<key> AGENCY_PROFILE=… node packages/cli/dist/index.js eval --agent --suite hard --provider nvidia --baseline .agency/eval-baseline-hard.json [--update-baseline]`.
  4. Sau khi xong: xác nhận config khôi phục literal gốc (KHÔNG còn placeholder) + `grep -rn "nvapi-" packages docs` = RỖNG + xóa temp. Baseline `.agency/*` gitignored → không commit.
- Chạy legacy (`--update-baseline`) rồi hardened (gate vs baseline). Đọc `report.results[]` (`--json`) để xem **per-task `rounds`** — `rounds>1` ở hardened = verify-loop đã kích hoạt + tự sửa (đó là tín hiệu giá trị, không chỉ success rate).

## 7. Cấm kỵ
- Không xóa/sửa khi chưa grep xác nhận 0 live importer — **quét CẢ `.ts` + `.tsx`** (đừng để false-dead như `semantic-orchestration.ts`).
- Không gộp các cặp "trùng tên cố ý" (mục 1).
- Không assert "green"; không commit khi `pnpm verify` chưa xanh.
- Không tạo file/tool/command trùng chức năng đã có — luôn tra "Canonical Homes" trước.

**Bắt đầu:** đọc mục 0 → chạy quét trùng (mục 1) → nêu rõ slice bạn chọn + lý do "không trùng + nối dây thật" → thực thi theo nhịp mục 5.
