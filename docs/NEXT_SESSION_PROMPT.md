# AgencyCLI — Prompt cho session sau (runtime-quality · token-efficiency · multimodal · TUI/UX)

> **Cách dùng:** dán nguyên khối từ dòng `---` dưới đây vào tin nhắn đầu của một phiên Claude Code mới trên repo này.
> Prompt **trỏ tới** docs/memory thật (không sao chép nội dung) nên không tự trở thành bản trùng lặp và luôn đồng bộ với docs.
> Số liệu trong prompt là ảnh chụp lúc soạn (2026-06-01, HEAD `aa14e48`) — phiên mới **xác nhận lại bằng docs + `git log` + `pnpm verify`**.

---

Bạn tiếp quản **AgencyCLI** (`D:\AgencyCLI`) — monorepo **pnpm, 16 package**, runtime agent **tự hành cục bộ** (KHÔNG phải chatbot), đang trong chiến dịch production-hardening + nâng chất lượng runtime. Hai "bệnh" cốt lõi của repo phải luôn cảnh giác: (1) **machinery xây xong nhưng không nối dây** (built-but-unwired) và (2) **trùng lặp logic/file/UI/kiến trúc**. Ba nguyên tắc bất di bất dịch: **không tạo trùng lặp · không xây thứ treo lơ lửng · VERIFY chứ đừng assert**.

## 0. ĐỌC TRƯỚC khi làm bất cứ gì (đúng thứ tự)
1. `memory/MEMORY.md` (1 dòng index) → `memory/agencycli-section8-overflow.md` (chi tiết "hướng mới" §8 + facts "đừng-điều-tra-lại") → `memory/agencycli-production-hardening.md` (nhật ký campaign + git chain).
2. **`docs/ROADMAP_HANDOFF.md` → §8 (banner đầu mục + §8.1–§8.11)** — đây là bản đồ việc. Mỗi item ghi **SỰ THẬT → LỖI → SỬA (+file)** từ source thật; mục đã ✅ thì đừng làm lại, mục 🔴/🟡/🟢 là việc còn lại. **ĐỪNG điều tra lại root cause — đã nằm trong §8.**
3. `docs/SESSION_HANDOFF_PROMPT.md` — luật chống-trùng-lặp (mục 1), wired-or-dead + **6 guard** (mục 2), verify-đừng-assert (mục 3), quy ước cờ/kiến trúc (mục 4), nhịp slice (mục 5), trạng thái mới nhất (mục 6), **xử lý BYOK key AN TOÀN (mục 6.1 — key chỉ ở env, KHÔNG lên đĩa)**.
4. `docs/PACKAGES.md` → **"Canonical Homes & No-Duplication Map"** + mục "Built-in tools" (**18 tool** trong MỘT `ToolRegistry`) — tra TRƯỚC khi thêm BẤT KỲ helper/module/tool/command nào. Có "nhà" rồi → tái dùng.

> **Bài học nhãn cũ:** khi kiểm dead-code phải grep CẢ `.ts` + `.tsx` + `.mts` (false-dead `semantic-orchestration.ts` từng wired qua `App.tsx`). Verify-đừng-assert áp cả cho **script audit tự viết** (background output có thể bị cắt). **Bài học mới (phiên này):** spawned background task có thể ghi đè working tree chính → `git add -A` gom NHẦM file lạ vào commit; LUÔN `git status`/`git show` soi diff trước/sau commit, tách commit nếu lẫn concern (soft-reset commit local chưa đẩy, KHÔNG amend lịch sử đã đẩy).

## 1. ĐÃ XONG phiên trước (2026-06-01, HEAD `aa14e48` trên `master`, cây sạch) — ĐỪNG làm lại
- **P0 + §8.5 + §8.11-A** (các phiên trước, vẫn đúng — chi tiết ROADMAP §8 banner): crash TUI `stripToolCalls` undefined-guard (`b83b55a`); overflow §8.1/8.2/8.3 minimax/NVIDIA (`106ee22`, catalog provider-aware→196608, estimator err-high, `reduceHistoryToFit` cắt thân + chặn ratchet 16887); §8.5 paste perf O(n²)→O(n) (`2172832`); §8.11-A `truncateToolResult` chết-trong-ESM→fix (`a550bd2`).
- **`e3622e7` §8.11-B(1) prompt-cache REORDER** (token win lớn nhất): `buildSystemPrompt` (`chat/prompt.ts`) tách segment **static** (identity+protocol+tool-docs) / **sessionAnchor** (goal pillars) / **variableTail** (route intent+context+memories+question); cờ `AGENCY_PROMPT_CACHE`/`promptCachePrefix` **off-legacy byte-identical** / **on-hardened static-prefix-first** → bật automatic prefix-cache cho MỌI openai-compatible (NVIDIA/openrouter/deepseek/Ollama), 0 đụng adapter. Pure reorder (length bất biến 2 mode).
- **`d68e3e0` §8.11-B(2) Anthropic cache_control**: `anthropic.ts buildSystemField(system,cache)` dùng chung 2 method → on gắn `cache_control:{type:"ephemeral"}` vào `body.system` (Anthropic KHÔNG có automatic prefix-cache). Opt `cacheSystemPrompt?` thêm vào `CompleteOptions` (providers leaf → không đọc core flags), core set từ `promptCachePrefix` ở `stream.ts`+`orchestrator.ts` (summarizer bỏ qua). GA Claude 3+ không cần beta header. **§8.11-B ĐÓNG TRỌN.**
- **`e142193` §8.11-C soften "5-APPROACHES RULE"**: cờ `AGENCY_SOFT_APPROACHES`/`softApproaches` (off-legacy giữ "exactly 5 distinct" verbatim / on-hardened "SOLUTION OPTIONS: a few 2–3, scaled to complexity, simple task → single recommendation"). Độc lập cờ reorder.
- **`9474075` deflake event-bus async-spill test**: test race `existsSync` trên file spill nửa-flush (full verify dưới tải → "Unexpected end of JSON input"). EventBus thêm `awaitSpill(refId)` (test/forensics-only; production vẫn fire-and-forget off hot-path), test await thay vì poll.
- **`aa14e48` 2 runtime fix từ ảnh user** (chi tiết ROADMAP §8 + memory topic): (1) **agent kẹt vòng lặp ghi FILE LỚN** — `write_file` ghi đè cả file trong 1 tool call → content vượt output-token-limit → cắt giữa chừng; thiếu append primitive nên model xoay PowerShell `Add-Content` → kẹt escaping → max-loop(15) → "tiếp tục" ghi lại từ đầu. **SỬA:** tool MỚI **`append_file`** (`tool-harness.ts`, appendFileSync, tạo nếu thiếu, vào `APPROVAL_GATED_TOOLS`+`FILE_WRITING_TOOLS`) + sửa mô tả `write_file` (chunked, cấm shell heredoc). Additive (không cờ), **18 tool**. (2) **egress chặn `fonts.gstatic.com`** → "SECURITY WARNING" doạ user cho Google Fonts (whitelist có `*.googleapis.com` CSS qua nhưng thiếu host font-binary đi kèm). **SỬA:** thêm `fonts.gstatic.com` vào default whitelist `egress-proxy.ts` (tối thiểu, gstatic khác vẫn block).
- **Baseline verify:** build 16/16, **REAL_EXIT_CODE=0**, **~2078 test** (core **367** · cli 573 · tui 124 · providers **852** · memory 36 · benchmark 18 · workspace 11 · security **36** · tooling 14 · governance 7 · skills-bridge 14 · context 6 · heuristics 6 · browser 5 · telemetry 9). **29 cờ** (xác nhận qua `agency status` / `flags.ts`).

> **Token-efficiency (§8.11-A/B/C) ĐÃ XONG hết phần không-cần-key.** Đo cache-hit THẬT cho cả prefix-cache (openai-compatible) lẫn Anthropic `cache_control` **cần BYOK key** (xem §6.1 SESSION_HANDOFF) — chạy hội thoại dài 2+ turn rồi đọc `usage` prompt_tokens (NVIDIA/openrouter) hoặc `cache_read_input_tokens` (Anthropic) để xác nhận hit > 0.

## 2. VIỆC KẾ TIẾP — chọn theo ưu tiên user (đã chẩn đoán sẵn trong ROADMAP §8)

### 🟠 §8.10 — TUI realtime activity + độ bền loop/resume (NHÁNH GỢI Ý — nối tiếp đúng pain user)
- **Gốc lớn nhất (A):** main turn KHÔNG phát event tool có cấu trúc — nó nhồi text `[SYSTEM: Executing tool…]` vào stream LLM (`chat/stream.ts:70` `formatToolCallNotice` + onDelta) rồi TUI **regex-parse lại** (`conversation/TraceTelemetry.tsx` + `utils/conversation/tool-labels.ts`). Vòng lossy → nhãn sai (`list_dir·short video`), 2 dòng/tool, global `lastToolTargets` Map (vỡ khi parallel). `subagent:progress` cấu trúc CHỈ fire khi `agentId` set = chỉ subagent.
- **(B)** `activityPhase` chỉ "routing→writing→idle", không đổi theo tool + heartbeat chỉ update khi `thought:emitted` → status kẹt "Writing 8m39s". **(D)** 4 bề mặt chồng chéo (`ExecutionPanel` cây phase HARDCODE giả). **(E)** `SystemActivityLine`≈`formatSystemActivityLine`+`toConciseTelemetry` = 3 bản render trùng.
- **SỬA (thứ tự A→B→D/E→C):** phát **1 event tool-lifecycle cấu trúc cho main turn** (tái dùng EventBus / `emitThought` — KHÔNG thêm bề mặt thứ 5) mang `{callId, toolName, target, status, durationMs}` → TUI lấy nhãn thẳng từ `tc.arguments` (hết regex), 1 dòng/tool transition theo `callId`, lái `activityPhase` realtime (read→Reading, grep→Searching, edit/append→Editing, exec→Running) → gộp bề mặt + dedup render.
- **⚠️ SUB-ITEM CÒN MỞ liên quan ảnh user (ưu tiên cao):** **max-loop-limit(15) + "tiếp tục" ghi LẠI từ đầu** = thiếu **completion-detection / resume tiến độ dở**. `append_file` (`aa14e48`) đã trị GỐC (model có primitive ghi nối-tiếp) nhưng độ bền vòng lặp là việc RIÊNG: khi chạm maxLoops nên (a) báo rõ "đã ghi X/Y, còn lại Z" thay vì cụt, (b) "tiếp tục" nên NỐI TIẾP file dở (đọc trạng thái thật: file đã có bao nhiêu) thay vì restart. Điều tra `maxLoops` ở turn loop (`chat/stream.ts`/`orchestrator.ts`) + cách "tiếp tục" tái dựng context. **Đây là việc bền-runtime user vừa gặp trực tiếp.**

### 🟡 §8.4 — Gửi ảnh / multimodal (đa tầng, làm END-TO-END kẻo treo type)
- `ChatMessage.content: string | ContentPart[]` (additive, default string byte-identical) + **1 helper canonical** `messageContentToText` ở providers (mọi string-op tái dùng — blast radius: 3 adapter `anthropic/google/openai-compatible` + core history + TUI). Adapter openai-compatible map `image_url` **CHỈ khi** `getModelSpec(model).capabilities.vision` (catalog đã phát hiện — tái dùng, không đoán). Estimator §8.3 ĐÃ đếm `IMAGE_PART_TOKENS` sẵn. **Producer:** TUI đính ảnh — tận dụng cơ chế `@`-path đã resolve+badge IMG (`PromptComposer`), đọc file→base64→ContentPart. ⚠️ Phải nối dây trọn vẹn (producer→type→adapter) — thêm `ContentPart[]` mà chưa có producer = built-but-unwired.

### 🟢 Token-efficiency còn lại + P2 (sau 8.10/8.4)
- **§8.11-D** rút tool-docs args (~1109 tok/turn — `formatToolDocs` liệt mọi arg mỗi turn; bớt arg cho tool hiển nhiên, giữ schema cho tool ít rõ). **§8.11-E** đổi tên `grep_file`/`grep_search` cho rõ (`search_in_file`/`search_workspace`) + ứng viên: **patch tool** (unified-diff, sửa nhiều hunk ít token hơn rewrite — bổ sung `batch_edit`/`ast_edit`/`append_file`) + **index-backed search** (`grep_search` đang walk lại cây; `loadIndex` đã có).
- **§8.6** memory recall precision@k (embedder `LocalDeterministicEmbedder` placeholder). **§8.7** index incremental/độ tươi. **§8.8** sandbox edge-case.
- **Promote `hardened`→default** (đích cuối): cần (i) eval delta sạch (BYOK key, corpus model fail-attempt-1 ổn định) + (ii) user OK rõ ràng. **KHÔNG tự flip.**

## 3. NHỊP MỖI SLICE (bắt buộc, lặp lại)
investigate (đọc + grep CẢ `.ts/.tsx/.mts`, tra Canonical Homes) → phân loại (trùng-thật vs distinct-cố-ý; wire vs delete) → thay đổi **tối thiểu, behavior-preserving, tái dùng canonical home** → **cờ trong `runtime/flags.ts` nếu đổi hành vi** (legacy byte-identical; risky=off-legacy/on-hardened; tốn-kém=opt-in off cả hai); **tool/command mới thuần additive = KHÔNG cần cờ** (vd `append_file`) → thêm/cập nhật test → **`pnpm verify` XANH (REAL_EXIT_CODE=0, 16 pkg)** → commit nhỏ trên `master` (cây sạch, KHÔNG amend/`--no-verify`, trailer `Co-Authored-By: Claude Opus 4.8`; soi `git status` trước `add` để khỏi gom file lạ) → sync living docs (ROADMAP/SESSION_HANDOFF/PACKAGES + thêm row Canonical Homes nếu có nhà mới) → cập nhật `memory/` + git chain. **Giữ 6 guard xanh** (skills↔manifest · agents↔prompt/seed · flags↔status · cycles-module · cycles-package · deps↔imports). Thêm skill/agent/flag/dep/tool mới → cập nhật manifest/seed/`buildFlagRows`/package.json/PACKAGES inventory trong CÙNG slice.

## 4. Cấm kỵ
- Không assert "green" — chạy `pnpm verify` trước khi commit (bỏ qua warning cố ý: failover/rate-limit/Playwright-missing/docker-unreachable).
- Không xóa/sửa khi chưa grep xác nhận 0 live importer (CẢ `.tsx`). Không gộp cặp "trùng tên cố ý" (`ReplayEngine`, `truncateText`, `ToolCall`, `GraphEdge`, `VerificationResult`, `AuditEntry`…).
- Không tạo helper/tool/estimator/command trùng cái đã có — tra Canonical Homes trước.
- Không tự `promote hardened→default` (cần BYOK eval delta sạch + user OK rõ ràng).
- BYOK key (nếu user cấp): theo `SESSION_HANDOFF §6.1` — backup config, placeholder `${VAR}`, `trap … EXIT` restore trong CÙNG lệnh bash, key chỉ ở env per-command, xác nhận `grep -rn "nvapi-\|sk-" packages docs` RỖNG sau khi xong.

**Bắt đầu:** đọc §0 → mở ROADMAP §8 (banner + §8.10/§8.4/§8.11-D-E) → hỏi/chốt user muốn nhánh nào (gợi ý: **§8.10 TUI realtime + độ bền loop/resume** = nối tiếp đúng pain user vừa gặp ở ảnh: event tool-lifecycle cấu trúc + sửa max-loop/resume; verify không cần key) → làm theo nhịp §3.
