# AgencyCLI — Prompt cho session sau (runtime-quality · token-efficiency · multimodal · TUI/UX)

> **Cách dùng:** dán nguyên khối từ dòng `---` dưới đây vào tin nhắn đầu của một phiên Claude Code mới trên repo này.
> Prompt **trỏ tới** docs/memory thật (không sao chép nội dung) nên không tự trở thành bản trùng lặp và luôn đồng bộ với docs.
> Số liệu trong prompt là ảnh chụp lúc soạn (2026-06-02, HEAD `f2fb68a`) — phiên mới **xác nhận lại bằng docs + `git log` + `pnpm verify`**.

---

Bạn tiếp quản **AgencyCLI** (`D:\AgencyCLI`) — monorepo **pnpm, 16 package**, runtime agent **tự hành cục bộ** (KHÔNG phải chatbot), đang trong chiến dịch production-hardening + nâng chất lượng runtime. Hai "bệnh" cốt lõi của repo phải luôn cảnh giác: (1) **machinery xây xong nhưng không nối dây** (built-but-unwired) và (2) **trùng lặp logic/file/UI/kiến trúc**. Ba nguyên tắc bất di bất dịch: **không tạo trùng lặp · không xây thứ treo lơ lửng · VERIFY chứ đừng assert**.

## 0. ĐỌC TRƯỚC khi làm bất cứ gì (đúng thứ tự)
1. `memory/MEMORY.md` (1 dòng index, đã gọn — chi tiết ở topic file) → `memory/agencycli-section8-overflow.md` (chi tiết "hướng mới" §8 + facts "đừng-điều-tra-lại") → `memory/agencycli-production-hardening.md` (nhật ký campaign + git chain).
2. **`docs/ROADMAP_HANDOFF.md` → §8 (banner đầu mục + §8.1–§8.11)** — đây là bản đồ việc. Mỗi item ghi **SỰ THẬT → LỖI → SỬA (+file)** từ source thật; mục đã ✅ thì đừng làm lại, mục 🔴/🟡/🟢 là việc còn lại. **ĐỪNG điều tra lại root cause — đã nằm trong §8.**
3. `docs/SESSION_HANDOFF_PROMPT.md` — luật chống-trùng-lặp (mục 1), wired-or-dead + **6 guard** (mục 2), verify-đừng-assert (mục 3), quy ước cờ/kiến trúc (mục 4), nhịp slice (mục 5), trạng thái mới nhất (mục 6), **xử lý BYOK key AN TOÀN (mục 6.1 — key chỉ ở env, KHÔNG lên đĩa)**.
4. `docs/PACKAGES.md` → **"Canonical Homes & No-Duplication Map"** + mục "Built-in tools" (**18 tool** trong MỘT `ToolRegistry`) — tra TRƯỚC khi thêm BẤT KỲ helper/module/tool/command nào. Có "nhà" rồi → tái dùng.

> **Bài học nhãn cũ:** khi kiểm dead-code phải grep CẢ `.ts` + `.tsx` + `.mts` (false-dead `semantic-orchestration.ts` từng wired qua `App.tsx`). Verify-đừng-assert áp cả cho **script audit tự viết** (background output có thể bị cắt). **Bài học mới (phiên này):** spawned background task có thể ghi đè working tree chính → `git add -A` gom NHẦM file lạ vào commit; LUÔN `git status`/`git show` soi diff trước/sau commit, tách commit nếu lẫn concern (soft-reset commit local chưa đẩy, KHÔNG amend lịch sử đã đẩy).

## 1. ĐÃ XONG phiên 2026-06-02 (HEAD `f2fb68a` trên `master`, cây sạch) — ĐỪNG làm lại (chi tiết ROADMAP §8 + memory topic)
> ⚠️ **NHẮC USER REBUILD/RESTART TUI:** các fix dưới ở `src` → TUI đang chạy `dist` CŨ vẫn lỗi cho tới khi `pnpm -r build` + khởi động lại. Ảnh self-kill/loop user gửi là **trước** fix.
- **§8.10 TUI realtime — ĐÓNG TRỌN** (5 slice): `1c6fc75` loop/resume (`buildIncompleteTurnNotice` ở turn-helpers — chạm maxLoops → notice 1 dòng `[SYSTEM:]` + file-list size đĩa thật, nối `llmText`→history; cờ `AGENCY_RESUME_CONTINUATION`) · `710abb0` A: `emitThought` tại main-turn tool loop + `describeToolActivity` (turn-helpers) → status realtime, ride cờ `cognitionStream` · `47c2667` B/D: `activityPhaseFromThought` lái `activityPhase` (hết kẹt "Writing">10s) + bỏ subtask GIẢ ExecutionPanel + `computeExecutionPhaseStatuses` · `0849922` E: trích `parseSystemActivityLine` (1 classifier) + XÓA dead `formatSystemActivityLine` · `fba316c` C: diệt nhãn sai `getGroundedTargetName` + gỡ `lastToolTargets` vestigial.
- **§8.11 token/clarity — ĐÓNG TRỌN** (A truncate + B caching + C approaches từ phiên trước; phiên này: `b40b1a6` **D** compact tool-docs args, cờ `AGENCY_COMPACT_TOOL_DOCS`, trích `describeZodArg`; `00f5312` **E** clarify mô tả grep cross-reference — CỐ Ý KHÔNG rename/patch/index, ghi lý do).
- **§8.7 index freshness** `09443d6`: XÓA nhánh `changedFiles` fast-path của `incrementalUpdateAsync` (built-but-unwired + buggy: chỉ duyệt existing.files → file mới không thêm); full-merge path đúng new/deleted/changed = path duy nhất; test real-fs khóa 3 mutation.
- **§8.8 sandbox/security — ĐÓNG TRỌN (5 fix)**: `c03b9a2` **self-kill HARD-refuse** · `71cbe78` **DockerSandbox timeout+output-cap** (parity native) · `ca2c954` **circuit-breaker fire-on-blocked-loop** (đếm `^Error[:\s]` là failure + reset mỗi turn + bound history) · `64e945a` **§8.8-A turn-loop HARD-break trên trip** (`consumeCircuitBreakerTrip` → fold `buildCircuitBreakerNotice` + break; KHÔNG cờ) **+ §8.8-B tolerate malformed `</tool_call>`** (`parseToolCalls` superset canonical). `fonts.gstatic.com` đã trong whitelist egress (`aa14e48`) — ảnh chặn font là do **dist cũ**, rebuild là hết.
- **Egress allowlist (`010880a`):** default whitelist + reputable read-only CDN `cdn.jsdelivr.net`/`unpkg.com`/`cdnjs.cloudflare.com`/`esm.sh` (host cụ thể, GET-only static = exfil tối thiểu) — agent scaffold web hết bị chặn vô cớ. security 38→39.
- **§8.10-F in-tool progress (commit này):** `startToolProgressHeartbeat` (turn-helpers) — `setInterval` re-narrate qua `describeToolActivity`→`emitThought` + elapsed suffix khi 1 tool chậm chạy lâu (status hết kẹt); `enabled`=`cognitionStream` truyền từ call-site (off→0 timer=legacy byte-identical), `unref`, stop trong `finally`, CẢ 2 path. KHÔNG cờ mới. → **§8.10 ĐÓNG TRỌN (6 slice).** core 403→405.
- **§9 Curated cross-session MARKDOWN memory (user-requested — đem cơ chế memory kiểu Claude-Code vào AgencyCLI):** `MarkdownMemoryStore` (@agency/memory) = `.agency/memory/` index `MEMORY.md` + topic file `<slug>.md` (frontmatter name/description/type), recall keyword|semantic budgeted, `user`/`feedback` luôn surface. Wire: tool MỚI `remember` (producer) + recall prepend vào `### SYSTEM HISTORICAL MEMORIES` (`loadFileMemoryBlock`) + PERSISTENT MEMORY PROTOCOL trong prompt, TẤT CẢ sau cờ MỚI `AGENCY_FILE_MEMORY` (off-legacy byte-identical: tool KHÔNG advertise, 0 recall). Phân biệt RÕ với SQLite episodic store tự động (đừng gộp — xem PACKAGES Canonical Homes). memory 36→49, core 405→409, **19 tool**.
- **Baseline verify:** build 16/16, **REAL_EXIT_CODE=0**, **~2161 test** (core **410** · cli 573 · tui **148** · providers 852 · memory **48** · benchmark 18 · workspace 11 · security **39** · tooling 14 · governance 7 · skills-bridge 14 · context 6 · heuristics 6 · browser 5 · telemetry 9). **32 cờ** (thêm `fileMemory`). **20 tool** (thêm `remember`+`forget`).
- **Harness memory (phiên này):** MEMORY.md của Claude gọn lại 29.9KB→2.4KB (hết overflow, load đủ) — detail dời xuống topic files; README thêm index "Process & Continuity" cho nhóm doc handoff.
- **⚠ NHẮC user:** §9 memory + §8.10 realtime đều sau cờ → mặc định profile `legacy` TẮT. Bật bằng `AGENCY_FILE_MEMORY=1` (+ `AGENCY_COGNITION_STREAM=1` cho realtime) hoặc `AGENCY_PROFILE=hardened`.

> **Token-efficiency (§8.11 A–E) + realtime (§8.10) + freshness (§8.7) + sandbox/security (§8.8) ĐÃ XONG phần không-cần-key.** Đo cache-hit THẬT (§8.11-B) vẫn cần BYOK key (xem §6.1 SESSION_HANDOFF).

## 2. VIỆC KẾ TIẾP — §8.10/§8.11/§8.7/§8.8 ĐÃ ĐÓNG; còn lại dưới đây (đã chẩn đoán trong ROADMAP §8)

### ✅ Follow-up trực tiếp từ ảnh user — ĐÃ XONG (`64e945a`, verify KHÔNG cần key)
- **(A) ✅ Hard-break turn loop trên circuit-breaker trip.** `executeTool` latch lý do (`lastCircuitBreakerTripReason`) → export `consumeCircuitBreakerTrip()` (read-and-clear); CẢ 2 turn loop gọi SAU khi append tool batch vào `turnHistory` → non-null thì fold `buildCircuitBreakerNotice` (nhà chung `turn-helpers.ts`) vào `llmText`(+onDelta) rồi **break**. KHÔNG cờ (precedent §8.8 safety). Test stop-tại-4-completions (không churn tới maxLoops).
- **(B) ✅ Malformed `</tool_call>` (minimax).** `parseToolCalls` regex nhận name nháy-đơn/spaced + whitespace close tag — SUPERSET canonical (output đúng byte-identical), call recoverable hết bị nuốt im; no-close-tag CỐ Ý không recover. Test +4. → §8.8 ĐÓNG TRỌN.

### 🟡 §8.4 — Gửi ảnh / multimodal (đa tầng, END-TO-END kẻo treo type; CẦN vision key verify e2e)
- `ChatMessage.content: string | ContentPart[]` (additive, default string byte-identical) + **1 helper canonical** `messageContentToText` ở providers (mọi string-op `.content` tái dùng — blast radius LỚN: turn-helpers `renderForSummary`/`reduceHistoryToFit`, prompt, memory, 3 adapter). Adapter openai-compatible map `image_url` **CHỈ khi** `getModelSpec(model).capabilities.vision` (catalog đã có — tái dùng). Estimator §8.3 ĐÃ đếm `IMAGE_PART_TOKENS`. **Producer:** TUI đính ảnh qua `@`-path (`PromptComposer` đã resolve+badge IMG) → đọc file→base64→ContentPart. ⚠️ Thêm `ContentPart[]` mà chưa có producer = built-but-unwired. **Verify body-shape không cần key (test request body); round-trip ảnh THẬT cần vision-capable BYOK key.**

### 🟢 P2 còn lại
- **§8.6** memory recall precision@k — embedder `LocalDeterministicEmbedder` là placeholder (feature-hashing, recall ngữ nghĩa yếu); thêm bài đo precision@k (không cần key) rồi cân nhắc **provider-embedder thật sau interface `Embedder`** (CẦN embeddings key, gate riêng giữ determinism eval/replay).
- **§8.7(4)** re-index đồng bộ trong dispatch (`updateKnowledgeGraphForFiles`→`incrementalUpdateAsync` full-walk) = ứng viên offload worker-thread (surface cao). **§8.10 in-tool progress** (heartbeat giữa 1 tool chậm grep/đọc file lớn — cần tool-level progress callback). **§8.8** native-mode security-warning mạnh hơn (ưu tiên thấp).
- **Promote `hardened`→default** (đích cuối): cần (i) eval delta sạch (BYOK key, corpus model fail-attempt-1 ổn định) + (ii) user OK rõ ràng. **KHÔNG tự flip.** Hiện 31 cờ off-legacy/on-hardened.

## 3. NHỊP MỖI SLICE (bắt buộc, lặp lại)
investigate (đọc + grep CẢ `.ts/.tsx/.mts`, tra Canonical Homes) → phân loại (trùng-thật vs distinct-cố-ý; wire vs delete) → thay đổi **tối thiểu, behavior-preserving, tái dùng canonical home** → **cờ trong `runtime/flags.ts` nếu đổi hành vi** (legacy byte-identical; risky=off-legacy/on-hardened; tốn-kém=opt-in off cả hai); **tool/command mới thuần additive = KHÔNG cần cờ** (vd `append_file`) → thêm/cập nhật test → **`pnpm verify` XANH (REAL_EXIT_CODE=0, 16 pkg)** → commit nhỏ trên `master` (cây sạch, KHÔNG amend/`--no-verify`, trailer `Co-Authored-By: Claude Opus 4.8`; soi `git status` trước `add` để khỏi gom file lạ) → sync living docs (ROADMAP/SESSION_HANDOFF/PACKAGES + thêm row Canonical Homes nếu có nhà mới) → cập nhật `memory/` + git chain. **Giữ 6 guard xanh** (skills↔manifest · agents↔prompt/seed · flags↔status · cycles-module · cycles-package · deps↔imports). Thêm skill/agent/flag/dep/tool mới → cập nhật manifest/seed/`buildFlagRows`/package.json/PACKAGES inventory trong CÙNG slice.

## 4. Cấm kỵ
- Không assert "green" — chạy `pnpm verify` trước khi commit (bỏ qua warning cố ý: failover/rate-limit/Playwright-missing/docker-unreachable).
- Không xóa/sửa khi chưa grep xác nhận 0 live importer (CẢ `.tsx`). Không gộp cặp "trùng tên cố ý" (`ReplayEngine`, `truncateText`, `ToolCall`, `GraphEdge`, `VerificationResult`, `AuditEntry`…).
- Không tạo helper/tool/estimator/command trùng cái đã có — tra Canonical Homes trước.
- Không tự `promote hardened→default` (cần BYOK eval delta sạch + user OK rõ ràng).
- BYOK key (nếu user cấp): theo `SESSION_HANDOFF §6.1` — backup config, placeholder `${VAR}`, `trap … EXIT` restore trong CÙNG lệnh bash, key chỉ ở env per-command, xác nhận `grep -rn "nvapi-\|sk-" packages docs` RỖNG sau khi xong.

**Bắt đầu:** đọc §0 → mở ROADMAP §8 (banner + §8.8/§8.4/§8.6) → **NHẮC user rebuild+restart TUI** để các fix `src` có hiệu lực → hỏi/chốt user muốn nhánh nào. Gợi ý verify-được-không-cần-key: **(A) hard-break turn loop trên circuit-breaker trip** (đóng nốt vòng churn user gặp ở ảnh) → rồi **(B) parseToolCalls robustness**. Mở §8.4 (multimodal) / §8.6 (provider-embedder) khi user cấp BYOK key (vision / embeddings) để verify e2e. → làm theo nhịp §3.
