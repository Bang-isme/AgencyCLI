{
  "name": "@agency/tooling",
  "version": "0.1.0",
  "description": "Agency CLI self-healing parser, schema coercion, and MCP supervisor",
  "license": "MIT",
  "type": "module",
  "engines": { "node": ">=22" },
  "files": ["dist"],
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "prepack": "pnpm run build"
  },
  "dependencies": {
    "@agency/contracts": "workspace:*",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "vitest": "^2.1.0",
    "typescript": "^5.6.0"
  },
  "publishConfig": { "access": "public" }
}
