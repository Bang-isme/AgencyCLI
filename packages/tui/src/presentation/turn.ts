/** Re-export shared presentation layer (single source of truth in @agency/core). */
export {
  formatChipsLine,
  parseAssistantContent,
  routeToChips,
  toPresentationTurn,
  type PresentationTurn,
  type RouteChip,
} from "@agency/core";
